import { createAction } from '@ngrx/store';

export const incrementCounter = createAction('counter incremented');
//          counter name                      ...just name like console.log
export const decrementCounter = createAction('counter incremented');
